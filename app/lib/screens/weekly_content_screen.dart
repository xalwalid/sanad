// Superseded by the 13-metric health recovery timeline (health_screen.dart).
// Kept as a harmless stub because this environment can't delete files.
// Safe to remove from the repo.
import 'package:flutter/material.dart';

@Deprecated('Replaced by HealthScreen (health_screen.dart)')
class WeeklyContentScreen extends StatelessWidget {
  const WeeklyContentScreen({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
