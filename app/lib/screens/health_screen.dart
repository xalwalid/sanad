// Superseded by recovery_screen.dart (RecoveryScreen + HealthDetailScreen).
// Kept as a harmless stub because this environment can't delete files.
// Safe to remove from the repo.
import 'package:flutter/material.dart';

@Deprecated('Use RecoveryScreen in recovery_screen.dart')
class HealthScreen extends StatelessWidget {
  const HealthScreen({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
